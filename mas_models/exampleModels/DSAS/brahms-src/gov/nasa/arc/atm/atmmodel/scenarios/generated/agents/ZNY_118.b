package gov.nasa.arc.atm.atmmodel.scenarios.generated.agents;

agent ZNY_118 memberof Tower { 
 initial_beliefs: 
(laGuardiaDepts.departure_queue(1) = plane_DEP-0 ); 
(laGuardiaDepts.departure_queue(2) = plane_DEP-1 ); 
(laGuardiaDepts.departure_queue(3) = plane_DEP-2 ); 
(laGuardiaDepts.departure_queue(4) = plane_DEP-3 ); 
(laGuardiaDepts.departure_queue(5) = plane_DEP-4 ); 
(laGuardiaDepts.departure_queue(6) = plane_DEP-5 ); 
(laGuardiaDepts.departure_queue(7) = plane_DEP-6 ); 
(laGuardiaDepts.departure_queue(8) = plane_DEP-7 ); 
(laGuardiaDepts.departure_queue(9) = plane_DEP-8 ); 
(laGuardiaDepts.departure_queue(10) = plane_DEP-9 ); 
(laGuardiaDepts.departure_queue(11) = plane_DEP-10 ); 
(laGuardiaDepts.departure_queue(12) = plane_DEP-11 ); 
(laGuardiaDepts.departure_queue(13) = plane_DEP-12 ); 
(laGuardiaDepts.departure_queue(14) = plane_DEP-13 ); 
     (current hasWaypoint LGA22); 
     (current hasWaypoint LGA31); 
     (current hasWaypoint KUTMI); 
     (current hasWaypoint RAMUP); 
     (current hasWaypoint BEGNZ); 
     (current hasWaypoint KWANN); 
     (current hasWaypoint THRON); 
(current.handoffWaypoint = LGA22); 
(current.handoffTo = ZNY_118); 
 initial_facts: 
(laGuardiaDepts.departure_queue(1) = plane_DEP-0 ); 
(laGuardiaDepts.departure_queue(2) = plane_DEP-1 ); 
(laGuardiaDepts.departure_queue(3) = plane_DEP-2 ); 
(laGuardiaDepts.departure_queue(4) = plane_DEP-3 ); 
(laGuardiaDepts.departure_queue(5) = plane_DEP-4 ); 
(laGuardiaDepts.departure_queue(6) = plane_DEP-5 ); 
(laGuardiaDepts.departure_queue(7) = plane_DEP-6 ); 
(laGuardiaDepts.departure_queue(8) = plane_DEP-7 ); 
(laGuardiaDepts.departure_queue(9) = plane_DEP-8 ); 
(laGuardiaDepts.departure_queue(10) = plane_DEP-9 ); 
(laGuardiaDepts.departure_queue(11) = plane_DEP-10 ); 
(laGuardiaDepts.departure_queue(12) = plane_DEP-11 ); 
(laGuardiaDepts.departure_queue(13) = plane_DEP-12 ); 
(laGuardiaDepts.departure_queue(14) = plane_DEP-13 ); 
     (current hasWaypoint LGA22); 
     (current hasWaypoint LGA31); 
     (current hasWaypoint KUTMI); 
     (current hasWaypoint RAMUP); 
     (current hasWaypoint BEGNZ); 
     (current hasWaypoint KWANN); 
     (current hasWaypoint THRON); 
(current.handoffWaypoint = LGA22); 
(current.handoffTo = ZNY_118); 
} 


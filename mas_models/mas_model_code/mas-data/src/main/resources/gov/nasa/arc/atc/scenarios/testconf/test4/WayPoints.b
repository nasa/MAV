object wpDeparture instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39.5);
 	(current.longitude = 70);
	initial_facts:
 	(current.latitude = 39.5);
 	(current.longitude = 70);
}

object A instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 40);
 	(current.longitude = 70.5);
	initial_facts:
 	(current.latitude = 40);
 	(current.longitude = 70.5);
}

object B instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39.5);
 	(current.longitude = 70.5);
	initial_facts:
 	(current.latitude = 39.5);
 	(current.longitude = 70.5);
}

object C instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 40);
 	(current.longitude = 69.5);
	initial_facts:
 	(current.latitude = 40);
 	(current.longitude = 69.5);
}

object D instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39.5);
 	(current.longitude = 69.5);
	initial_facts:
 	(current.latitude = 39.5);
 	(current.longitude = 69.5);
}

object E instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39);
 	(current.longitude = 70);
	initial_facts:
 	(current.latitude = 39);
 	(current.longitude = 70);
}

object F instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39);
 	(current.longitude = 71);
	initial_facts:
 	(current.latitude = 39);
 	(current.longitude = 71);
}

object G instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39);
 	(current.longitude = 69);
	initial_facts:
 	(current.latitude = 39);
 	(current.longitude = 69);
}

object H instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 38.5);
 	(current.longitude = 70);
	initial_facts:
 	(current.latitude = 38.5);
 	(current.longitude = 70);
}

object I instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 38);
 	(current.longitude = 70);
	initial_facts:
 	(current.latitude = 38);
 	(current.longitude = 70);
}


object wpArrival instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 37.5);
 	(current.longitude = 70);
	initial_facts:
 	(current.latitude = 37.5);
 	(current.longitude = 70);
}

object DEPA instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39);
 	(current.longitude = 71.5);
	initial_facts:
 	(current.latitude = 39);
 	(current.longitude = 71.5);
}

object A instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39);
 	(current.longitude = 71);
	initial_facts:
 	(current.latitude = 39);
 	(current.longitude = 71);
}

object DEPB instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 40);
 	(current.longitude = 70.5);
	initial_facts:
 	(current.latitude = 40);
 	(current.longitude = 70.5);
}

object B instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39.38858);
 	(current.longitude = 70.5);
	initial_facts:
 	(current.latitude = 39.38858);
 	(current.longitude = 70.5);
}

object DEPC instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39);
 	(current.longitude = 69.5);
	initial_facts:
 	(current.latitude = 39);
 	(current.longitude = 69.5);
}

object C instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39);
 	(current.longitude = 70);
	initial_facts:
 	(current.latitude = 39);
 	(current.longitude = 70);
}

object MERGE instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39);
 	(current.longitude = 70.5);
	initial_facts:
 	(current.latitude = 39);
 	(current.longitude = 70.5);
}

object D instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 38);
 	(current.longitude = 70.5);
	initial_facts:
 	(current.latitude = 38);
 	(current.longitude = 70.5);
}


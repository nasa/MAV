object DEP instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 40);
 	(current.longitude = 70.5);
	initial_facts:
 	(current.latitude = 40);
 	(current.longitude = 70.5);
}

object A instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 40);
 	(current.longitude = 71);
	initial_facts:
 	(current.latitude = 40);
 	(current.longitude = 71);
}

object B instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 40);
 	(current.longitude = 70);
	initial_facts:
 	(current.latitude = 40);
 	(current.longitude = 70);
}

object C instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 39);
 	(current.longitude = 70.5);
	initial_facts:
 	(current.latitude = 39);
 	(current.longitude = 70.5);
}

object D instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 38);
 	(current.longitude = 70.5);
	initial_facts:
 	(current.latitude = 38);
 	(current.longitude = 70.5);
}

object E instanceof Waypoint {
	initial_beliefs:
 	(current.latitude = 38);
 	(current.longitude = 68);
	initial_facts:
 	(current.latitude = 38);
 	(current.longitude = 68);
}
